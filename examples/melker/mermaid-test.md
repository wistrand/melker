Test of mermaid melker conversion

The following mermaidf block will be transformed into an inline
melker 
```
<graph type="mermaid">
   flowchart LR
   A --> B --> C
</graph>
```

and this displayed as a rendered chart

```mermaid
        flowchart LR
          A --> B --> C

```

